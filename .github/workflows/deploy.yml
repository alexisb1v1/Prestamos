name: Deploy Frontend NeoCobros

on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Docker image
        run: |
          docker build \
            --build-arg NEXT_PUBLIC_API_URL=/api/v1 \
            -t ghcr.io/${{ github.repository_owner }}/neocobros-app:${{ github.sha }} \
            -t ghcr.io/${{ github.repository_owner }}/neocobros-app:latest .

      - name: Push Docker image
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/neocobros-app:${{ github.sha }}
          docker push ghcr.io/${{ github.repository_owner }}/neocobros-app:latest


